## TODO

## Important

[x] Query parameter support. (Idea: Query should be set as part of the URL?)

[x] Environment variable support.

[x] Document how environment variables must be set. ie. `{{env:EXAMPLE}}`

[x] More assertion types e.g is_not_equal, is_greater_than, is_less_than, in_array, status_code

[x] Add delay support

[ ] Reference properties within the assertions. (Consider a `var` directive ie. users can define a variable, assign a value to it and reference the variable across the test file)

## Nice to have

[x] Random value generators e.g. UUID

[ ] Custom helper or assertion functions

[x] YAML support

[ ] Randomising - picking a random value from an array set.

[ ] More assertion types.
